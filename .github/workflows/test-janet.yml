name: Run Janet Tests
on: [push, pull_request]

jobs:
  janet-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t janet-tests .

      - name: Run tests
        run: docker run --rm janet-tests
