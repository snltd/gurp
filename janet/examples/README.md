## Doer Examples

`ensure` and (where applicable) `remove` examples for all doers. These examples
are used in:

- Janet unit tests in `janet/test/doers`
- Markdown documentation generated by `janet/bin/docgen.janet`
- Rust unit tests in `doers/src`

Each doer has its own directory, containing numbered files. Each file should
contain a single resource description.
